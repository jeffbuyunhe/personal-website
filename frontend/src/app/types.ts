export interface Navigation {
    name: string;
    link: string;
}

export interface CenterText {
    text: string;
    img: string;
    imgHovered: string;
}

export interface CardData {
    text: string;
    img: string;
}
