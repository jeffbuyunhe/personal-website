export interface Navigation {
    name: string;
    link: string;
    active: boolean;
}

export interface CenterText {
    text: string;
    img: string;
}

export interface CardData {
    text: string;
    img: string;
}
